{
  "scripts": {
    "build": "echo 'No frontend build in root. Use npm run build in frontend/ if needed.'"
  },
  "dependencies": {
    "keen-slider": "^6.8.6"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.4",
    "autoprefixer": "^10.5.0"
  }
}
