export const contatoStyles = {
  wrapper: "flex flex-col gap-8 px-4 py-12 max-w-5xl mx-auto",
  heading: "text-3xl font-bold text-gray-900",
  subheading: "text-gray-500 text-base",

  // Layout
  grid: "grid grid-cols-1 md:grid-cols-2 gap-8",

  // Informações
  infoSection: "flex flex-col gap-6",
  infoCard: "flex flex-col gap-1",
  infoLabel: "text-xs font-semibold text-gray-400 uppercase tracking-wider",
  infoValue: "text-base text-gray-700 font-medium",
  infoLink: "text-base text-gray-700 font-medium hover:text-pink-500 transition-colors",

  // Botões
  whatsappBtn: "flex items-center justify-center gap-2 bg-green-500 hover:bg-green-600 text-white font-semibold py-3 px-6 rounded-xl transition-colors text-sm w-full",
  ifoodBtn: "flex items-center justify-center gap-2 bg-red-500 hover:bg-red-600 text-white font-semibold py-3 px-6 rounded-xl transition-colors text-sm w-full",
  btnGroup: "flex flex-col gap-3 mt-2",

  // Horários
  hoursGrid: "flex flex-col gap-2",
  hoursRow: "flex justify-between text-sm",
  hoursDays: "text-gray-600",
  hoursTime: "text-gray-800 font-medium",

  // Mapa
  mapWrapper: "rounded-2xl overflow-hidden shadow border border-gray-100 h-full min-h-[400px]",
  map: "w-full h-full min-h-[400px]",
}
